<!-- Include Head -->
<%- include('../partials/head', {title: 'CPanel Da Gym'}) %>

<!-- Include Header -->
<%- include('../partials/header') %>

<!-- Profile stile.css line:920 -->

<div class="container profileEditForm">
    <h2 class="profileTitle">Usuarios Registrados</h1>
    <div class="container profileBody">
        <ul class="container userEditProfileForm" >
            <% users.forEach(user => { %>
             <li>
                <p class="userData">
                    <%= user.first_name %> <%= user.last_name %>
                </p>
                <form action="/admin/user/edit/<%= user.id %>?_method=PUT" enctype="multipart/form-data" class="container userEditProfileForm" method="POST">
                    <select name="role" id="">
                        <option value="0">Elija un Rol</option>
                        <option value="1">Cliente</option>
                        <option value="2">Profesor</option>
                        <option value="3">Administrador</option>
                    </select>
                    <button type="submit" class="btn btn-primary">Guardar cambios</button>
                </form>
                <form action="/admin/user/delete/<%= user.id %>?_method=DELETE" class="" id="deleteForm" method="POST">
                    <button type="submit" class="btn btn-danger deleteUser">Eliminar Perfil</button>
                </form>
                
            </li>
            <% }) %>

        <!-- <div class="profileBtn">

        </div> -->
    </div>
</div>

<!-- Include Footer -->
<%- include('../partials/footer') %>
